export const metadata = {
  title: "Canyon Strategic Wealth — Welcome Packet",
  description: "Your personalized welcome packet from Canyon Strategic Wealth",
};

export default function PacketLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
